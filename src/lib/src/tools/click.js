export default async function click(){
  const app = document.getElementById(await '#app');
  app.addEventListener(await 'click', function(event) {
    const target = event.target;
    return target

  });


}